// If - Else Statements

    const fname = 'Elif';
    const age = 13;
    const isStudent = true;
    const schold = 'primary school';

    if(fname ===  'Elif') {
        console.log(`Hello ${fname}.`);
    }

    if(isStudent) {
        console.log(isStudent);
    }else {
        console.log(!isStudent);
    }